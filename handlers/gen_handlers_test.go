// THIS FILE IS AUTOGENERATED. DO NOT EDIT.
// Regen by running 'go generate' in the repo root.

package handlers

import (
	"testing"
	"time"

	"github.com/AshokShau/gotdbot"
)

func TestGeneratedHandlers(t *testing.T) {
	d := gotdbot.NewDispatcher(&gotdbot.Client{}, nil)

	func() {
		called := make(chan bool, 1)
		h := NewUpdateAccentColors(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateAccentColors{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateAccentColors not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateActiveEmojiReactions(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateActiveEmojiReactions{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateActiveEmojiReactions not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateActiveGiftAuctions(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateActiveGiftAuctions{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateActiveGiftAuctions not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateActiveLiveLocationMessages(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateActiveLiveLocationMessages{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateActiveLiveLocationMessages not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateActiveNotifications(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateActiveNotifications{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateActiveNotifications not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateAgeVerificationParameters(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateAgeVerificationParameters{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateAgeVerificationParameters not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateAnimatedEmojiMessageClicked(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateAnimatedEmojiMessageClicked{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateAnimatedEmojiMessageClicked not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateAnimationSearchParameters(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateAnimationSearchParameters{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateAnimationSearchParameters not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateApplicationRecaptchaVerificationRequired(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateApplicationRecaptchaVerificationRequired{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateApplicationRecaptchaVerificationRequired not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateApplicationVerificationRequired(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateApplicationVerificationRequired{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateApplicationVerificationRequired not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateAttachmentMenuBots(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateAttachmentMenuBots{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateAttachmentMenuBots not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateAuthorizationState(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateAuthorizationState{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateAuthorizationState not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateAutosaveSettings(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateAutosaveSettings{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateAutosaveSettings not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateAvailableMessageEffects(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateAvailableMessageEffects{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateAvailableMessageEffects not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateBasicGroup(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateBasicGroup{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateBasicGroup not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateBasicGroupFullInfo(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateBasicGroupFullInfo{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateBasicGroupFullInfo not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateBusinessConnection(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateBusinessConnection{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateBusinessConnection not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateBusinessMessageEdited(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateBusinessMessageEdited{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateBusinessMessageEdited not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateBusinessMessagesDeleted(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateBusinessMessagesDeleted{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateBusinessMessagesDeleted not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateCall(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateCall{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateCall not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateChatAccentColors(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateChatAccentColors{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateChatAccentColors not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateChatAction(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateChatAction{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateChatAction not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateChatActionBar(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateChatActionBar{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateChatActionBar not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateChatActiveStories(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateChatActiveStories{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateChatActiveStories not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateChatAddedToList(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateChatAddedToList{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateChatAddedToList not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateChatAvailableReactions(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateChatAvailableReactions{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateChatAvailableReactions not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateChatBackground(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateChatBackground{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateChatBackground not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateChatBlockList(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateChatBlockList{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateChatBlockList not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateChatBoost(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateChatBoost{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateChatBoost not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateChatBusinessBotManageBar(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateChatBusinessBotManageBar{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateChatBusinessBotManageBar not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateChatDefaultDisableNotification(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateChatDefaultDisableNotification{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateChatDefaultDisableNotification not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateChatDraftMessage(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateChatDraftMessage{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateChatDraftMessage not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateChatEmojiStatus(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateChatEmojiStatus{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateChatEmojiStatus not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateChatFolders(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateChatFolders{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateChatFolders not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateChatHasProtectedContent(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateChatHasProtectedContent{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateChatHasProtectedContent not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateChatHasScheduledMessages(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateChatHasScheduledMessages{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateChatHasScheduledMessages not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateChatIsMarkedAsUnread(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateChatIsMarkedAsUnread{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateChatIsMarkedAsUnread not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateChatIsTranslatable(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateChatIsTranslatable{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateChatIsTranslatable not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateChatLastMessage(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateChatLastMessage{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateChatLastMessage not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateChatMember(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateChatMember{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateChatMember not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateChatMessageAutoDeleteTime(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateChatMessageAutoDeleteTime{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateChatMessageAutoDeleteTime not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateChatMessageSender(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateChatMessageSender{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateChatMessageSender not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateChatNotificationSettings(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateChatNotificationSettings{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateChatNotificationSettings not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateChatOnlineMemberCount(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateChatOnlineMemberCount{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateChatOnlineMemberCount not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateChatPendingJoinRequests(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateChatPendingJoinRequests{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateChatPendingJoinRequests not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateChatPermissions(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateChatPermissions{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateChatPermissions not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateChatPhoto(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateChatPhoto{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateChatPhoto not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateChatPosition(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateChatPosition{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateChatPosition not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateChatReadInbox(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateChatReadInbox{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateChatReadInbox not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateChatReadOutbox(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateChatReadOutbox{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateChatReadOutbox not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateChatRemovedFromList(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateChatRemovedFromList{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateChatRemovedFromList not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateChatReplyMarkup(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateChatReplyMarkup{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateChatReplyMarkup not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateChatRevenueAmount(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateChatRevenueAmount{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateChatRevenueAmount not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateChatTheme(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateChatTheme{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateChatTheme not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateChatTitle(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateChatTitle{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateChatTitle not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateChatUnreadMentionCount(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateChatUnreadMentionCount{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateChatUnreadMentionCount not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateChatUnreadReactionCount(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateChatUnreadReactionCount{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateChatUnreadReactionCount not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateChatVideoChat(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateChatVideoChat{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateChatVideoChat not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateChatViewAsTopics(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateChatViewAsTopics{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateChatViewAsTopics not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateConnectionState(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateConnectionState{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateConnectionState not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateContactCloseBirthdays(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateContactCloseBirthdays{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateContactCloseBirthdays not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateDefaultBackground(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateDefaultBackground{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateDefaultBackground not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateDefaultPaidReactionType(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateDefaultPaidReactionType{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateDefaultPaidReactionType not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateDefaultReactionType(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateDefaultReactionType{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateDefaultReactionType not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateDeleteMessages(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateDeleteMessages{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateDeleteMessages not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateDiceEmojis(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateDiceEmojis{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateDiceEmojis not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateDirectMessagesChatTopic(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateDirectMessagesChatTopic{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateDirectMessagesChatTopic not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateEmojiChatThemes(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateEmojiChatThemes{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateEmojiChatThemes not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateFavoriteStickers(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateFavoriteStickers{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateFavoriteStickers not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateFile(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateFile{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateFile not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateFileAddedToDownloads(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateFileAddedToDownloads{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateFileAddedToDownloads not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateFileDownload(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateFileDownload{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateFileDownload not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateFileDownloads(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateFileDownloads{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateFileDownloads not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateFileGenerationStart(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateFileGenerationStart{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateFileGenerationStart not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateFileGenerationStop(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateFileGenerationStop{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateFileGenerationStop not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateFileRemovedFromDownloads(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateFileRemovedFromDownloads{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateFileRemovedFromDownloads not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateForumTopic(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateForumTopic{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateForumTopic not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateForumTopicInfo(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateForumTopicInfo{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateForumTopicInfo not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateFreezeState(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateFreezeState{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateFreezeState not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateGiftAuctionState(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateGiftAuctionState{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateGiftAuctionState not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateGroupCall(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateGroupCall{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateGroupCall not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateGroupCallMessageLevels(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateGroupCallMessageLevels{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateGroupCallMessageLevels not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateGroupCallMessagesDeleted(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateGroupCallMessagesDeleted{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateGroupCallMessagesDeleted not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateGroupCallMessageSendFailed(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateGroupCallMessageSendFailed{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateGroupCallMessageSendFailed not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateGroupCallParticipant(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateGroupCallParticipant{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateGroupCallParticipant not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateGroupCallParticipants(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateGroupCallParticipants{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateGroupCallParticipants not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateGroupCallVerificationState(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateGroupCallVerificationState{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateGroupCallVerificationState not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateHavePendingNotifications(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateHavePendingNotifications{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateHavePendingNotifications not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateInstalledStickerSets(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateInstalledStickerSets{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateInstalledStickerSets not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateLanguagePackStrings(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateLanguagePackStrings{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateLanguagePackStrings not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateLiveStoryTopDonors(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateLiveStoryTopDonors{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateLiveStoryTopDonors not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateMessageContent(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateMessageContent{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateMessageContent not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateMessageContentOpened(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateMessageContentOpened{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateMessageContentOpened not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateMessageEdited(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateMessageEdited{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateMessageEdited not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateMessageFactCheck(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateMessageFactCheck{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateMessageFactCheck not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateMessageInteractionInfo(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateMessageInteractionInfo{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateMessageInteractionInfo not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateMessageIsPinned(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateMessageIsPinned{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateMessageIsPinned not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateMessageLiveLocationViewed(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateMessageLiveLocationViewed{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateMessageLiveLocationViewed not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateMessageMentionRead(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateMessageMentionRead{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateMessageMentionRead not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateMessageReaction(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateMessageReaction{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateMessageReaction not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateMessageReactions(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateMessageReactions{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateMessageReactions not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateMessageSendAcknowledged(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateMessageSendAcknowledged{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateMessageSendAcknowledged not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateMessageSendFailed(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateMessageSendFailed{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateMessageSendFailed not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateMessageSendSucceeded(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateMessageSendSucceeded{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateMessageSendSucceeded not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateMessageSuggestedPostInfo(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateMessageSuggestedPostInfo{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateMessageSuggestedPostInfo not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateMessageUnreadReactions(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateMessageUnreadReactions{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateMessageUnreadReactions not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateNewBusinessCallbackQuery(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateNewBusinessCallbackQuery{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateNewBusinessCallbackQuery not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateNewBusinessMessage(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateNewBusinessMessage{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateNewBusinessMessage not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateNewCallbackQuery(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateNewCallbackQuery{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateNewCallbackQuery not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateNewCallSignalingData(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateNewCallSignalingData{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateNewCallSignalingData not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateNewChat(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateNewChat{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateNewChat not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateNewChatJoinRequest(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateNewChatJoinRequest{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateNewChatJoinRequest not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateNewChosenInlineResult(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateNewChosenInlineResult{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateNewChosenInlineResult not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateNewCustomEvent(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateNewCustomEvent{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateNewCustomEvent not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateNewCustomQuery(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateNewCustomQuery{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateNewCustomQuery not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateNewGroupCallMessage(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateNewGroupCallMessage{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateNewGroupCallMessage not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateNewGroupCallPaidReaction(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateNewGroupCallPaidReaction{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateNewGroupCallPaidReaction not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateNewInlineCallbackQuery(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateNewInlineCallbackQuery{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateNewInlineCallbackQuery not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateNewInlineQuery(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateNewInlineQuery{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateNewInlineQuery not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateNewMessage(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateNewMessage{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateNewMessage not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateNewOauthRequest(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateNewOauthRequest{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateNewOauthRequest not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateNewPreCheckoutQuery(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateNewPreCheckoutQuery{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateNewPreCheckoutQuery not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateNewShippingQuery(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateNewShippingQuery{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateNewShippingQuery not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateNotification(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateNotification{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateNotification not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateNotificationGroup(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateNotificationGroup{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateNotificationGroup not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateOption(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateOption{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateOption not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateOwnedStarCount(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateOwnedStarCount{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateOwnedStarCount not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateOwnedTonCount(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateOwnedTonCount{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateOwnedTonCount not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdatePaidMediaPurchased(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdatePaidMediaPurchased{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdatePaidMediaPurchased not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdatePendingTextMessage(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdatePendingTextMessage{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdatePendingTextMessage not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdatePoll(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdatePoll{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdatePoll not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdatePollAnswer(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdatePollAnswer{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdatePollAnswer not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateProfileAccentColors(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateProfileAccentColors{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateProfileAccentColors not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateQuickReplyShortcut(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateQuickReplyShortcut{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateQuickReplyShortcut not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateQuickReplyShortcutDeleted(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateQuickReplyShortcutDeleted{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateQuickReplyShortcutDeleted not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateQuickReplyShortcutMessages(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateQuickReplyShortcutMessages{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateQuickReplyShortcutMessages not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateQuickReplyShortcuts(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateQuickReplyShortcuts{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateQuickReplyShortcuts not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateReactionNotificationSettings(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateReactionNotificationSettings{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateReactionNotificationSettings not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateRecentStickers(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateRecentStickers{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateRecentStickers not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateSavedAnimations(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateSavedAnimations{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateSavedAnimations not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateSavedMessagesTags(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateSavedMessagesTags{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateSavedMessagesTags not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateSavedMessagesTopic(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateSavedMessagesTopic{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateSavedMessagesTopic not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateSavedMessagesTopicCount(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateSavedMessagesTopicCount{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateSavedMessagesTopicCount not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateSavedNotificationSounds(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateSavedNotificationSounds{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateSavedNotificationSounds not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateScopeNotificationSettings(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateScopeNotificationSettings{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateScopeNotificationSettings not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateSecretChat(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateSecretChat{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateSecretChat not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateServiceNotification(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateServiceNotification{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateServiceNotification not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateSpeechRecognitionTrial(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateSpeechRecognitionTrial{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateSpeechRecognitionTrial not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateSpeedLimitNotification(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateSpeedLimitNotification{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateSpeedLimitNotification not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateStakeDiceState(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateStakeDiceState{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateStakeDiceState not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateStarRevenueStatus(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateStarRevenueStatus{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateStarRevenueStatus not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateStickerSet(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateStickerSet{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateStickerSet not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateStory(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateStory{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateStory not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateStoryDeleted(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateStoryDeleted{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateStoryDeleted not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateStoryListChatCount(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateStoryListChatCount{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateStoryListChatCount not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateStoryPostFailed(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateStoryPostFailed{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateStoryPostFailed not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateStoryPostSucceeded(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateStoryPostSucceeded{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateStoryPostSucceeded not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateStoryStealthMode(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateStoryStealthMode{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateStoryStealthMode not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateSuggestedActions(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateSuggestedActions{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateSuggestedActions not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateSupergroup(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateSupergroup{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateSupergroup not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateSupergroupFullInfo(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateSupergroupFullInfo{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateSupergroupFullInfo not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateTermsOfService(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateTermsOfService{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateTermsOfService not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateTonRevenueStatus(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateTonRevenueStatus{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateTonRevenueStatus not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateTopicMessageCount(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateTopicMessageCount{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateTopicMessageCount not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateTrendingStickerSets(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateTrendingStickerSets{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateTrendingStickerSets not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateTrustedMiniAppBots(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateTrustedMiniAppBots{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateTrustedMiniAppBots not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateUnconfirmedSession(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateUnconfirmedSession{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateUnconfirmedSession not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateUnreadChatCount(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateUnreadChatCount{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateUnreadChatCount not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateUnreadMessageCount(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateUnreadMessageCount{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateUnreadMessageCount not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateUser(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateUser{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateUser not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateUserFullInfo(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateUserFullInfo{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateUserFullInfo not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateUserPrivacySettingRules(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateUserPrivacySettingRules{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateUserPrivacySettingRules not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateUserStatus(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateUserStatus{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateUserStatus not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateVideoPublished(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateVideoPublished{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateVideoPublished not called")
		}
	}()

	func() {
		called := make(chan bool, 1)
		h := NewUpdateWebAppMessageSent(nil, func(b *gotdbot.Client, ctx *gotdbot.Context) error {
			called <- true
			return nil
		})
		d.AddHandler(h)
		d.ProcessUpdate(&gotdbot.UpdateWebAppMessageSent{})
		select {
		case <-called:
		case <-time.After(100 * time.Millisecond):
			t.Errorf("Handler for UpdateWebAppMessageSent not called")
		}
	}()

}
